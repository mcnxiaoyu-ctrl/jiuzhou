# 腾讯云 COS 配置（可选）
# 配置后构建时会自动上传静态资源到 COS

# COS 存储桶名称（必需，格式: bucket-appid）
export COS_BUCKET=

# COS 区域（默认: ap-guangzhou）
export COS_REGION=ap-guangzhou

# COS 上的路径前缀（默认: /jiuzhou）
export COS_PATH=/jiuzhou

# CDN 加速域名（可选，不填则使用 COS 默认域名）
export CDN_DOMAIN=
